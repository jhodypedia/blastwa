// small helper functions
function toastSuccess(msg){ Swal.fire({icon:'success', title: msg, toast:true, position:'top-end', timer:1500, showConfirmButton:false}); }
function toastError(msg){ Swal.fire({icon:'error', title: msg, toast:true, position:'top-end', timer:2000, showConfirmButton:false}); }
